import 'package:flutter/material.dart';

class PrintIdCardScreen extends StatelessWidget {
  const PrintIdCardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Print ID Card Screen'),
      ),
    );
  }
}
